# Thuva Tharma Dotfiles

These are config files to set up a system the way I like it.

## Installation

    git clone git@github.com:thuva/dotfiles.git ~/.dotfiles
    cd ~/.dotfiles
    rake install
